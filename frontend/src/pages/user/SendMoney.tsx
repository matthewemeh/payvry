import BackButton from '../../components/BackButton';

const SendMoney = () => {
  return (
    <main>
      <h1 className='font-semibold text-[20px] leading-[26px] text-center pt-14'>Send Money</h1>
      <BackButton />
    </main>
  );
};

export default SendMoney;
